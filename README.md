# BJSS Pair Programming Exercise

## Setup

* Clone this repo and ensure Python version > 3.6
* Alternatively, make virtualenv with requirements.txt and activate

## To run tests

* Run in terminal in root directory `python -m test_funcs -vv`
